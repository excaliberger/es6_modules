class Car {
    constructor(carArgs) {
        this.id = carArgs.id;
        this.make = carArgs.make;
        this.model = carArgs.model;
        this.year = carArgs.year;
    }
}

export default Car;